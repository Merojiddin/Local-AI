# Local AI Toolbox — Python 3.12, Apple Silicon only.
# Pinned to versions verified to work together (July 2026).
mlx-audio==0.4.5
mlx-lm==0.31.3
mlx-vlm==0.5.0          # 0.6.x needs starlette>=1.0 which conflicts with gradio
gradio==5.50.0
psutil>=6,<8
pypdfium2>=4.30         # render PDF pages for Vision/OCR
pypdf>=5,<7             # extract PDF text for document search
python-docx>=1.1,<2
pyobjc-framework-Vision>=11   # macOS built-in OCR
